# Built binaries at the repo root only — unanchored patterns would also
# swallow cmd/foreman/ and cmd/foreman-explorer/ (issue #16).
/foreman
/foreman-explorer

# Finder droppings — unanchored on purpose, Finder leaves them anywhere.
.DS_Store
